import React from 'react'
import { CFooter } from '@coreui/react'

const AppFooter = () => {
  return (
    <CFooter>
      <div>
        <h1>Footer</h1>
      </div>
    </CFooter>
  )
}

export default React.memo(AppFooter)
